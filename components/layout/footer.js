import classes from "./footer.module.scss";

const Footer = () => {
  return (
    <footer className={classes.footer}>
      <div>contact me</div>
      <div>source code</div>
    </footer>
  );
};

export default Footer;
